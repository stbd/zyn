# todo
 * Fix user authority hash usage
 * Implement automatic release for locks when lock goes out of scope
 * Think about when to close connection to client
 * Go trough client to make sure trace levels are consistent
 * Make sure naming of pages is consistent around system (not block)
 * Refactor
  * Filesystem
   * Only N files should be stored for each folder
 * Docker configuration for production
 * Possibility to store protocol messages to file, to make debugging easier

## Issues
 * It is possible to create file/folder with name that already exists under parent